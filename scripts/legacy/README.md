Superseded exploratory scripts, kept for provenance. Nothing in the
paper pipeline (project/trubric/) or the data pipeline imports these;
they may reference pre-reorg paths (figures/*.json) and old series
names (qubric). See scripts/ for the live pipeline and
project/trubric/scripts/ for the per-artifact figure scripts.
